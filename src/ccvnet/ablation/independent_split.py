from __future__ import annotations


def run(config: dict) -> None:
    """Run independent-split ablations.

    This is intentionally outside the main baseline path. The main baseline uses
    the fine-group shared split used by the main paper experiments.
    """
    raise NotImplementedError("Independent split ablation migration pending.")


