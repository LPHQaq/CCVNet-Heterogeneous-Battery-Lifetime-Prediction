extends: ../default.yaml
experiment:
  name: main_baseline
  result_set: all
  description: Major plus ablation benchmark on the full training setting.
  cache_name: all_main_baseline
  required_frames: [overall_metric_df, dataset_metric_df, prediction_df, skipped_df]

