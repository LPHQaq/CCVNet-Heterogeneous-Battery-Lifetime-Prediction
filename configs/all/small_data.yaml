extends: ../default.yaml
experiment:
  name: small_data
  result_set: all
  description: Major plus ablation data-scarcity benchmark.
  cache_name: all_small_data
  required_frames: [overall_metric_df, dataset_metric_df, prediction_df]
  training_modes: [total split small data, per-dataset small data, fine-group small data]

