# Data

Place local datasets here. Raw and processed data files are ignored by git by default.

Expected release-time folders:

- `processed/features/`: processed feature CSV files;
- `processed/cvd_curves/`: early-cycle CVD curve records;
- `external/`: third-party datasets downloaded by the user.
